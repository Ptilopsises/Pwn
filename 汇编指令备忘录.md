# x86-64 ELF 栈相关指令备忘录

## 栈基本规则

- 栈向低地址增长
- 栈顶寄存器：rsp
- 栈帧基址：rbp
- 指令指针：rip
- 64位下一个栈槽通常为 8 字节

---

## push src

作用：将一个值压入栈顶

效果：

- rsp -= 8
- [rsp] = src

拆一层：

```asm
sub rsp, 8
mov [rsp], src
```

---

## pop dst

作用：从栈顶弹出一个值到目标

效果：

- dst = [rsp]
- rsp += 8

拆一层：

```asm
mov dst, [rsp]
add rsp, 8
```

---

## call target

作用：保存返回地址，然后跳转到目标地址

效果：

- rsp -= 8
- [rsp] = 下一条指令地址
- rip = target

拆一层：

```asm
sub rsp, 8
mov [rsp], next_ip
jmp target
```

---

## ret

作用：从栈顶取出返回地址并跳转

效果：

- rip = [rsp]
- rsp += 8

拆一层：

```asm
mov temp, [rsp]
add rsp, 8
jmp temp
```

---

## leave

作用：销毁当前函数栈帧

效果：

- rsp = rbp
- rbp = [rsp]
- rsp += 8

拆一层：

```asm
mov rsp, rbp
pop rbp
```

---

## jmp target

作用：直接跳转到目标地址

效果：

- rip = target

特点：

- 不保存返回地址
- 不操作栈

---

## 一个补充：为什么你在逆向里总看到 `leave; ret`

因为很多函数长这样：

```asm
push rbp
mov rbp, rsp
sub rsp, xxx
...
leave
ret
```

它们组成一个完整闭环：

- `push rbp`：保存旧栈帧
- `mov rbp, rsp`：建立新栈帧
- `sub rsp, xxx`：开局部变量空间
- `leave`：恢复栈帧
- `ret`：回到调用者

这就是经典函数模板，老熟人了，pwn 里见到都快见出感情了。

---

你要的话，我下一条可以继续帮你补一份 **“函数序言 / 尾声 / 栈帧结构图”**，我可以直接画成：

- 调用前
- `call` 后
- `push rbp` 后
- `leave; ret` 后

那样会比纯文字更直观。
