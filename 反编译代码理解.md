# GPT反编译口诀

## 0. 总纲：先看“怎么用”，再猜“是什么”

**口诀：**
**“先看它被拿去干嘛（call / free / puts / memcpy），再反推它是什么类型。”**
反编译最怕从类型出发硬猜，最稳的是从“用途”倒推。

---

## 1. 指针与数组：`&x + i` 基本就是 `x[i]`

**口诀：**
**“&数组 + i，八成在取第 i 个；再来个 *，就是拿值。”**

常见形态：

* `*((T*)&arr + i)` ≈ `((T*)&arr)[i]`
* 如果 `arr` 本来就是指针数组，那就更像 `arr[i]`

你这题里：

* `*((_DWORD *)&notelist + i)` ≈ `notelist[i]`

---

## 2. 星号数=“解引用次数”

**口诀：**
**“几个 *，就往里扒几层皮。”**

例子：

* `p`：地址
* `*p`：p 指向的内容
* `**p`：p 指向的内容里再指向的内容

你的经典句：

* `**((_DWORD **)&notelist + i) = print_note_content;`
  两个 `*` = “先取 `notelist[i]`（指针），再写 `*(notelist[i])`（结构体首字段）”

---

## 3. 结构体字段：`a1 + 4/8/0xC` 就是字段偏移

**口诀：**
**“看到 +4、+8、+C：别慌，这是字段偏移在招手。”**

32位最常见：

* `a1 + 0`：第一个字段
* `a1 + 4`：第二个字段
* `a1 + 8`：第三个字段

你这题里：

* `puts(*(char **)(a1 + 4))`
  说明 `a1+4` 是 `char*`（content 指针）

---

## 4. 函数指针：看到“解引用后被 call”，九成是 vtable/回调

**口诀：**
**“能被 call 的数据，通常就是函数指针。”**

常见形态：

* `(*(func_t*)p)(args...)`
* `(**(func_t**)p)(args...)`（多一层通常是“结构体里第一个字段是函数指针”）

你这题的 print：

* `(**((int (__cdecl ***)(_DWORD))&notelist + idx))(notelist[idx])`
  读法：

1. 取 `notelist[idx]` 得到 note 指针
2. 取 note[0] 当函数指针
3. 调用，参数是 note 本身

=> 直接还原：`note->print(note)`

---

## 5. `malloc/free` 读法：看“被 free 的是谁”就能反推结构体布局

**口诀：**
**“谁被 free，谁就是指针字段。”**

例如：

* `free(*(void **)(p + 4))`
  => `p+4` 必然存着一个堆指针（content）
* `free(p)`
  => `p` 自己也是堆块（结构体）

---

## 6. `read/gets/memcpy`：第二个参数常是“目标地址”

**口诀：**
**“read(0, X, n)：X 往往是缓冲区指针；X 来自哪就说明哪是可写区。”**

你题里：

* `read(0, *(void **)(note + 4), size)`
  => `note+4` 是 content 指针，指向可写 buffer

---

## 7. “反编译变量名不可信”，要信控制流与 API

**口诀：**
**“变量名全是假的，API 调用是真的。”**

比如你看到 `count = count;` 这种鬼东西：

* 很可能是反编译器没恢复出真正的全局变量/寄存器含义
* 但 `malloc/free/puts/read` 的语义非常可靠

---

## 8. 越界判断的经典坑：`idx >= idx` 这种就是反编译失败信号

**口诀：**
**“看到 idx >= idx 这种自我比较，说明原本是 idx >= MAX 或 idx >= count，被反编译器搞丢了。”**

你题里：

* `if ( count < 0 || count >= count )`
  大概率原意是：
  * `count >= 5` 或 `count >= note_count`

---

## 9. 读题三连：堆题永远先问这三句

**口诀：**

1. **“free 后指针清零了吗？”** （没清零 = UAF）
2. **“能不能再次 malloc 抢占同大小 chunk？”** （重占位）
3. **“有没有函数指针/虚表/HOOK 可劫持？”** （控制流）

这题：第1条直接命中 ✅

---

# 最后送你一张“速查小抄”（超常用）

* `a1 + 4` → 结构体第二字段（32位）
* `*(a1)` 被当函数调用 → 结构体首字段是函数指针
* `*((T*)&arr + i)` → `arr[i]`
* `free(*(p + k))` → `p+k` 是指针字段
* `read(0, p, n)` → `p` 是可写 buffer
* `idx>=idx` → 反编译没恢复出边界常量

---

你要不要拿你题里那句最丑的间接调用（print_note 那一行）来做一次“逐字符翻译练习”？我可以带你把它从外星语还原回 `note->print(note)`，顺便教你以后看到类似表达怎么秒读。
