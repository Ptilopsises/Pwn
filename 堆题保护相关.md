我看了你这份笔记，核心时间线基本是对的，不过有一处要稍微“打个补丁”：

不是 Ubuntu 22.04 才开始没法打 hook，准确说是 glibc 2.34 开始，这些 malloc hooks 就“名存实亡”了。
glibc 2.34 的发布说明写得很直接：__malloc_hook、__realloc_hook、__memalign_hook、__free_hook 被移出 API，兼容符号可能还在，但对 glibc 功能已经没有效果。 ￼

下面我按 “堆保护机制 / hook 变化 / 对应 Ubuntu 版本” 给你整理成一张实战表。

---

## 1. 你这份 md 里提到的关键保护机制时间线

### ① tcache 引入

- glibc 2.26 开始引入 tcache（thread cache）
- 这会显著改变很多老题利用方式，因为：
- 小块 free 以后优先进 tcache
- fastbin / unsortedbin 的行为会被“截流”
- 你文档里写“ubuntu 17.10 之后引入 tcache”，这个判断是对的。

也就是说：
- Ubuntu 17.10 → glibc 2.26
- Ubuntu 18.04 → glibc 2.27，所以 18.04 已经是 tcache 时代了

---

### ② tcache double free 检查（key 机制）

- glibc 2.29 开始，tcache 增加了 key 字段来检测 double free
- 在这之前（2.28 及以下），tcache dup 更舒服，老题经常直接开冲
- 从 2.29 开始：
- tcache_entry 增加 key
- free() 时会检查这块 chunk 是否已经在 tcache 链表里
- 这一点和你 md 里的说法一致。 ￼

---

### ③ Safe-Linking

- glibc 2.32 开始引入 Safe-Linking
- 主要保护对象：
- tcache
- fastbin
- 本质是把单链表指针做异或保护：
- stored_ptr = (chunk_addr >> 12) ^ real_next
- 影响：
- 传统直接改 fd/next 的打法难度上升
- 往往要先有 heap leak
- 这点你文档也写对了。 ￼

---

### ④ malloc/free hook 失效 / 被取消

- glibc 2.34 开始：
- __malloc_hook
- __free_hook
- __realloc_hook
- __memalign_hook
这些老朋友基本就退出历史舞台了
- 更准确地说：
- 从 API 中移除
- 兼容符号可能还存在
- 但已经不再对 glibc 分配逻辑生效
- 所以实战里一般直接记：
- glibc < 2.34：还能考虑 hook 劫持
- glibc >= 2.34：别惦记 hook 了，换打法
- 你 md 里写“Ubuntu 22.04 删去了几乎所有 hook”，本质上是因为 Ubuntu 22.04 默认 glibc 2.35，而 2.35 已经在 2.34 之后了，所以表现为“22.04 不能玩 hook”。但真正分界线是 glibc 2.34。 ￼

---

## 2. 对应到 Ubuntu 版本，打题最常用的速查表

这是你最需要背的那张表，建议直接刻进 DNA：

Ubuntu 版本	默认 glibc 大版本	堆利用上要重点记的事
Ubuntu 16.04	2.23	没 tcache，老派 fastbin / unsortedbin / unlink / hook 思路常见
Ubuntu 17.10	2.26	tcache 首次引入
Ubuntu 18.04	2.27	有 tcache，hook 还能用，很多经典题卡在这个版本
Ubuntu 19.04	2.29	tcache double free key 检查开始出现
Ubuntu 20.04	2.31	仍然能玩 hook，但 tcache 时代保护已经更强了
Ubuntu 20.10	2.32	Safe-Linking 引入，tcache/fastbin 指针不再裸奔,tcache_prethread大小变为0x290
Ubuntu 21.04	2.33	Safe-Linking 继续存在，hook 还能勉强算可用时代末期
Ubuntu 21.10	2.34	malloc/free hooks 失效/取消的关键分界线
Ubuntu 22.04	2.35	不能再靠 __malloc_hook / __free_hook 打法
Ubuntu 24.04	2.39	更现代的 libc 环境，老 hook 流派直接寄

其中：

- Ubuntu 18.04 = glibc 2.27 这点非常经典。 ￼
- Ubuntu 20.04 = glibc 2.31。 ￼
- Ubuntu 22.04 = glibc 2.35。 ￼

---

## 3. 打题时可以怎么粗暴记忆

可以直接记成四个时代：

### 时代 A：glibc 2.23 及更早

- 没 tcache
- 老式 fastbin / unsortedbin / unlink / house 系打法很常见
- hook 也能玩

对应典型系统：

- Ubuntu 16.04

---

### 时代 B：glibc 2.26 ~ 2.28

- tcache 刚引入
- 但还没有 tcache key double free 检查
- 这阶段很多题目很好打，属于“tcache 刚上线，防御还没补齐”的黄金年代

对应典型系统：

- Ubuntu 17.10
- Ubuntu 18.04

---

### 时代 C：glibc 2.29 ~ 2.33

- tcache key 检查有了
- Safe-Linking 在 2.32 加入
- 但 hook 还没死透
- 所以常见路线是：
- leak libc
- tcache poisoning / UAF / unsorted leak
- 最后打 __free_hook / __malloc_hook

对应典型系统：

- Ubuntu 19.04
- Ubuntu 20.04
- Ubuntu 20.10
- Ubuntu 21.04

这其实就是很多现代堆题最爱出的区间，经典又不至于太上古。

---

### 时代 D：glibc 2.34+

- hook 基本退役
- 不能再指望 __free_hook=system 这种一招鲜
- 更常见要转向：
- FSOP
- ROP / setcontext
- vtable / _IO_FILE
- rtld / exit handlers / fini_array / stack pivot / ORW 等路线

对应典型系统：

- Ubuntu 21.10
- Ubuntu 22.04
- Ubuntu 24.04

---


**从 glibc 2.41 开始，free 一个“smallbin 范围”的 chunk 时，glibc 不再默认先丢进 unsortedbin，而是直接挂进对应的 smallbin。**

这个改动对应的补丁题目就叫  **“malloc: send freed small chunks to smallbin”** 。补丁邮件在 2024-07-08 发出，源码提交记录显示它在 2024-11-29 进入 glibc 主线；而官方发布时间上，glibc 2.40 是 2024-07-22，glibc 2.41 是 2025-01-30，所以 **第一个正式包含这个行为的稳定版，就是 2.41** 。

## 4. 结合你这份 md，给你的最终版结论

你可以把你文档里的“版本结论”整理成下面这版，更稳：

堆保护机制版本总结

- glibc 2.26：引入 tcache
- glibc 2.29：引入 tcache double free 检查（key）
- glibc 2.32：引入 Safe-Linking，保护 tcache / fastbin 单链表指针
- glibc 2.34：__malloc_hook / __free_hook / __realloc_hook / __memalign_hook 失效/被取消

Ubuntu 对应关系

- Ubuntu 16.04 → glibc 2.23
- Ubuntu 17.10 → glibc 2.26
- Ubuntu 18.04 → glibc 2.27
- Ubuntu 20.04 → glibc 2.31
- Ubuntu 20.10 → glibc 2.32
- Ubuntu 21.10 → glibc 2.34
- Ubuntu 22.04 → glibc 2.35
- Ubuntu 24.04 → glibc 2.39  ￼

---

## 5. 给你一个最实用的打题口令版

直接背这个就够了：

16.04 没 tcache；18.04 有 tcache 也能打 hook；20.04 还是 hook 黄金尾巴；20.10 开始 Safe-Linking；21.10/22.04 以后 hook 基本别想了。

这句几乎能覆盖你 80% 的堆题开局判断。

要是你愿意，我下一条可以直接帮你把你这个 堆相关.md 里“版本与保护机制”这一节，整理成一段更适合放博客里的正式版 markdown 内容，你直接粘进去就能用了。
